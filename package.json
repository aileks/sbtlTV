{
  "name": "netv-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop IPTV player with mpv",
  "scripts": {
    "dev": "pnpm --filter @netv/electron dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.0.0"
}
